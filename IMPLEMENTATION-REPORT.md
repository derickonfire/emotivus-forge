# Forge 0.570 implementation report

## Scope

P1-01 and P1-02 of the Durable Core roadmap: active runtime import reachability, bounded command-path observation, and complete active top-level path classification.

## Result

All 88 runtime modules are reachable through at least one declared active surface when ordinary CLI imports, standalone evidence tools, and verification entry points are considered together. No module is removal-authorized solely by this map. Every active top-level path now has a durable-goal, support, packaging, migration, web, testing, or reference classification.

## Context digest (Goal 1 redirection)

0.556 delivers the first-contact context digest — the evidence-driven redirection of Goal 1 recorded in `planning/OBSERVED-MISS-project-intelligence.md` and validated by a 12-project spectrum test. Forge now content-scans ordinary source for hardcoded secrets at orientation (not only filename-flagged files), reads a one-line description plus entry points and run/test commands from the project, falls identity back through go.mod/Cargo.toml/README, prints a measured Resume-vs-repo token comparison, collapses empty governance ceremony, and orients before requiring an objective. Re-running the spectrum, mean cold-model usefulness rose from 1.83/5 to 2.67/5 and blocked-before-value dropped from 11/12 to 0/12. 0.557 completes the project-intelligence pass: objective detection now recognizes explicit `## Objective`/`Goal:` headings; a deterministic architecture/layout summary (top dirs, primary source, packages, tests, central files) is surfaced; and secret coverage adds Stripe/generic live-token BLOCK rules and content-scans extensionless credential files. 0.558 replaces the hardcoded if/elif language dispatch with a ranked ecosystem resolver (source-count + manifest + app-framework boosts) so polyglot/framework projects dispatch correctly (Laravel -> php artisan; Java -> mvn; Ruby -> rake); extends identity to pom.xml/gemspec/static-site <title>; broadens test discovery (root test.js, *_test.go) and secret coverage (.npmrc/npm auth tokens). Validated on the batch-1 failure cases. 0.559 turns to the Goal-1 provable-truth core, driven by a 15-agent adversarial field test (`planning/G1-FIELD-TEST-OBSERVED-MISSES.md`): imported authority baselines are corroborated against a chain-verified authorization event or demoted to UNCORROBORATED; change detection reports bounded confidence when a file was compared by size+mtime rather than hash; and derived identity/objective/description/run-commands are labeled inferred rather than asserted as fact. The spine held under the test (tests never shown as passing, document signals never confer authority, self-metrics never authorization). A 15-agent re-test then confirmed all five 0.559 fixes held and surfaced one dominant deeper-gate root cause: Forge's ledger corroboration chain is unkeyed and travels inside the project, so a fabricated-consistent chain can spoof in-instance authorship. 0.560 responds honestly and bounded: corroboration wording and the authority TRUTH_BOUNDARY now state the unkeyed residual (removing the over-claim "in this instance's ledger"), and the Ship candidate-unchanged claim carries the bounded size+mtime-only qualifier. Full cryptographic instance-binding is recorded as the next increment (planning/G1-RETEST-0559-OBSERVED-MISSES.md). Additive; suite unchanged at 523/54. 0.561 adds a genuine read-only consultation mode (`run --read-only`, `resume --read-only`): a single state-directory redirect (`state_root`/`redirect_state`) makes every ForgePaths-based read/write and the storage lock resolve to a disposable directory outside the project, so Forge reads the real project bytes and prior state but writes nothing into the project tree — proven by a whole-tree byte-identity regression. This is what lets Forge advise on a shared or third-party repository under a strict read-only bound without adopting it. Suite grows additively to 529/55. 0.562 begins cryptographic instance-binding (`core/instance_key.py`): authority-baseline authorization events are signed with a per-instance HMAC key stored outside any project tree (Forge home, `FORGE_HOME`-overridable), the signature excluded from the ledger canonical hash so the chain is unchanged. Corroboration becomes tri-state (instance-bound / self-consistent / uncorroborated) and only instance-bound is release-eligible, so an imported package's fabricated-but-consistent chain — which cannot carry a signature under the verifying instance's key — stays self-consistent and never release-eligible (the 0.560 residual, now closed). Honest limits (key not human, machine-compromise, unsigned-when-home-unavailable) are stated in TRUTH_BOUNDARY; multi-party peer enrollment is the next increment. Suite grows additively to 530/55. 0.563 completes multi-party instance-binding: an owner-provisioned shared collaboration secret is held out-of-band in each trusted party's Forge home (`forge adopt --generate-collaboration-secret`/`--enroll-collaboration-secret`), signing prefers it when enrolled, and verification trusts the instance's own key plus the collaboration key. Authorizations are then mutually instance-bound across enrolled parties, while a party without the secret sees only self-consistent (never release-eligible) — the enforceable basis for a cross-model collaboration, proven by a two-home regression. Suite grows additively to 531/55. 0.564 extends instance-binding to artifact provenance (parity with authority): artifact-provenance-recorded is a signed kind, evaluate_artifact_provenance corroborates each active record against its signed event (instance-bound / self-consistent / uncorroborated), and scoped_check asserts CONFIRMED only for instance-bound records — a byte-matching but unsigned or imported record is honest as current yet not authenticated. This closes the last place the self-consistent-is-not-authentic class lived. 0.565 binds imported native-gate evidence to its source tree fingerprint (DG-8): import_native_evidence captures and persists source_tree_fingerprint, and evidence_validity.effective_native_validity downgrades readers (resume, self-currency) to stale-source-changed when the current tree differs — evidence for tree A no longer reads current for tree B. The remaining recorded finding (DG-7, same-version collision) was reviewed and did not survive verification (the guard already keys off differing bytes and the declared version), so no speculative fix was made. Suite grows additively to 533/55. 0.566 certifies Goal 1 COMPLETE: every completion-rule dimension (exact identity, authority, lineage, evidence binding, adversarial package rejection, package authorization) is verified by a passing adversarial regression, inventoried in planning/G1-COMPLETION.md, and the public package passes 533/55 twice deterministically from its own extracted bytes (P2-08/P2-09 satisfied). Release authorization remains false. Suite unchanged at 533/55. 0.567 opens the Goal-3 cross-model evolution work: forward migration is guaranteed (and regression-locked) to preserve fields this schema does not recognize — an older/foreign package keeps unknown top-level and nested fields verbatim — and core/forward_compat.py reports preserved-but-unrecognized settings fields, surfaced conditionally in the Resume Brief and never interpreted or trusted. Goal 1 stays COMPLETE. Suite grows additively to 536/56. 0.568 continues G3 with vendor-neutral continuity (P4-01): load_session_context now rejects model-instruction and vendor-identity keys (system_prompt/system/instructions/model_instructions/persona/prompt(s)/tools/functions/model/provider/vendor) alongside raw-transcript keys, so the continuity kernel stores project truth, not model instructions, and a different model can consume it without inheriting another model's directives. Structural key-based screening; no heuristic content scanning. Suite grows additively to 537/56. 0.569 adds explicit component lifecycle records (P4-03): lifecycle.record_lifecycle_transition validates a project-owned contract (component, disposition in retain/fold/freeze/retire/replace, authority, reason; fold/replace require a successor; replace requires preserved_invariants) and emits an append-only, chain-verified component-lifecycle-transition ledger event; lifecycle_transition_summary gives an audit view, surfaced conditionally in the Resume Brief, and a new adopt flag records it. Component evolution across model generations is now auditable. Suite grows additively to 542/57. A 12-agent adversarial G3 field test then found two over-assertions, both closed in 0.570: (GM-2) component-lifecycle-transition is added to SIGNED_KINDS and lifecycle_transition_summary runs classify_signature per event, labeling each transition instance-bound vs self-consistent so an imported/forged transition is never counted as authentic; (GM-1) load_session_context now screens accepted free-text values (objective/decisions/next_action/...) for narrow, high-precision prompt-injection and vendor-directive patterns rather than keys alone, and the vendor-neutral truth boundary is softened to what the code enforces. Suite grows additively to 544/57. The formal P2-01 schema chunk remains active.
