# Forge website

The generated public site intentionally contains exactly four pages: Home, Run Forge, Project Truth, and Continuity & Evidence. Design assets remain in `assets/`; downloads remain exact files rather than extra pages. Generate from `FORGE-PRODUCT.json` with `build_site.py`.
