# Forge 0.558 certification

Forge 0.556 is certified for **523 focused public-neutral regressions** across 54 deterministic isolated modules; the certified suite is unchanged, as 0.558 replaces the hardcoded language dispatch with a ranked ecosystem resolver (primary-language ranking across Python/Node/Go/Rust/Java/Ruby/PHP + static/notebook), extends identity to pom.xml/gemspec/<title>, broadens test discovery and secret coverage (.npmrc/npm tokens) — additively, without adding or removing behavior tests.

Final certification must execute from source, standalone public bytes, development-edition bytes, and the website-embedded runtime. It does not establish owner authorization, release readiness, independent review, physical-device evidence, or product efficacy.
