# Forge 0.568 certification

Forge is certified for **537 focused public-neutral regressions** across 56 deterministic isolated modules; the certified suite grows additively, as 0.568 makes the continuity kernel explicitly vendor-neutral — a distilled session digest that carries model-instruction or vendor-identity keys is rejected on intake, so it stores project truth rather than model instructions and a different model can consume it without inheriting another model's directives. One regression was added; no existing behavior test was changed or removed. Goal 1 remains COMPLETE; release authorization remains false.

Final certification must execute from source, standalone public bytes, development-edition bytes, and the website-embedded runtime. It does not establish owner authorization, release readiness, independent review, physical-device evidence, or product efficacy.
