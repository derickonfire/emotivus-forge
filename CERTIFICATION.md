# Forge 0.573 certification

Forge is certified for **546 focused public-neutral regressions** across 58 deterministic isolated modules. 0.573 is a field-usefulness and anti-bloat release: a genuine read-only consultation on a real project surfaced three defects where Forge behaved as a blocker rather than an advisor — all three are fixed and regression-locked (a pending decision fork is now advisory instead of a "stop before changing the project" blocker; the objective resolver obeys a document's own historical/superseded banner instead of scraping a stale heading; test/acceptance/gate harnesses are discovered instead of reported as zero). Internal self-consistency gates that enforced bookkeeping ceremony rather than a truthful claim were removed (per-chunk timebox format/range, retired-percentage guards, exact website-nav labels, same-file goal-status duplication); every check that prevents a real reader-facing misstatement was kept. Three new regression tests were added and four ceremony tests removed, so the certified count is unchanged and no existing behavior test was altered. Goal 1 is COMPLETE, Goal 3's foundation is certified/CONTINUOUS; release authorization remains false.

Final certification must execute from source, standalone public bytes, development-edition bytes, and the website-embedded runtime. It does not establish owner authorization, release readiness, independent review, physical-device evidence, or product efficacy.
