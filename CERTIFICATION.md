# Forge 0.575 certification

Forge is certified by a comprehensive suite of focused, public-neutral regressions across deterministic, isolated modules that the package runs from its own extracted bytes — the live count is reported by the self-test runner (`python3 -m emotivus_forge self-test`), not asserted here as a hand-maintained number. 0.575 advances Goal 2 (one-command session continuity): a cold model can now leave a session with the same one command it uses to enter — `Run Forge --close <digest>` records the durable Session Close, refreshes continuity, surfaces the exact next action, and optionally exports a portable continuity bundle, without switching to a separate close or ship workflow. It reuses the existing scoped-Check and Session-Close machinery (wiring, not a new subsystem) and is regression-locked end to end. The narrative-integrity check verifies version, schema, required-path, and download-checksum relationships; the count is computed live (0.574). Goal 1 is COMPLETE, Goal 3's foundation is certified/CONTINUOUS; Goal 2 remains open (enter and leave now work in one command; release-candidate routing and the scored cross-vendor cold-trial gate remain). Release authorization remains false.

Final certification must execute from source, standalone public bytes, development-edition bytes, and the website-embedded runtime. It does not establish owner authorization, release readiness, independent review, physical-device evidence, or product efficacy.
