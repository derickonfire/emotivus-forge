# Forge 0.554 findings

| ID | Status | Finding |
|---|---|---|
| F-554-001 | CORRECTED | Four evidence modules (`evidence_kit`, `attestation_kit`, `external_evidence`, `browser_evidence`) each carried byte-identical copies of the deterministic archive/hash primitives (`canonical_bytes`, `pretty_bytes`, `sha256_bytes`, `sha256_file`, `safe_member`, symlink test, `zip_info`, `ZIP_TIMESTAMP`) and bypassed the shared `core/common.py`. The primitives were centralized in `common.py` and the four modules routed through them, preserving bare public names as re-exports where tests import them. Every content digest is unchanged; behavior identical. |
| F-554-002 | CORRECTED | Three release modules (`release_package`, `release_proof`, `release_distribution`) each re-implemented an identical `_current_build_id`. Added `project_identity.current_build_id` — the truth-boundary identity read — and routed all three through it. Pure function, identical output; release evaluations unchanged. |
| F-554-003 | CORRECTED | Four more modules (`artifact_collision`, `build_attestation`, `narrative_integrity`, `workspace_integrity`) re-implemented the chunked file digest that already exists as `common.sha256_file`. Routed each to the shared helper. |
| F-554-004 | RETAINED TRUTH | The fold removed ~94 net lines across 13 modules while keeping the certified suite at 523/523 across 54 modules, `narrative_integrity` PASS, `check_progress` PASS, and the runtime inventory at 88 modules with zero unreachable and zero unclassified. No module was deleted (deletion is P1-06); reachability is not deletion authority. Release authorization remains `false`. |
| F-554-005 | CORRECTED | The kit-archive hygiene block was byte-identical between `verify_portable_evidence_kit` and `verify_owner_attestation_kit`. Extracted `common.verify_archive_hygiene(archive, problems) -> (names, root_name, manifest_name, manifest)` and routed both through it (~27 lines removed), preserving every emitted problem string and each module's distinct `kit_id`/schema tail. This completes the safe fold surface of P1-04. |
| F-554-006 | DISTINCT-BY-DESIGN | The receipt evaluators (`_review_receipt` / `_receipt_evaluation` / `_channel_receipt`), the kit verifiers' identity logic, and the `remote_release`/`runtime_proof` HTTP machinery were investigated and judged genuinely distinct, not redundant: they diverge on failure model, reason cardinality, missing-file handling, schema/`kit_id` identity, and scheme policy. Merging them would risk evidence identity — the invariant P1-04 forbids — so they are correctly left distinct. P1-04 is COMPLETE: everything that truly overlapped is folded. |
| F-554-007 | CORRECTED | P1-05: the capability-activation contract required three attestations (`focused_regressions`, `native_advantage`, `allow_repairs`) and non-empty `authority`/`scope` that no runtime gate reads. They were removed / made optional, reducing the record to the minimal enabled/reason/scope/evidence shape. Every enforced safety gate is preserved verbatim (activatable-only, containment, budget caps, runtime-proof egress allowlist and per-recipe binding, fingerprint drift → reactivation-required). The no-repair/no-credential guarantee is structural in the services, not a contract field. Verified: a minimal contract now activates and an out-of-range budget is still rejected; all 523 tests green with zero test edits beyond the active-chunk expectation. |

Open findings: **0** (F-554-006 records a validated distinct-by-design boundary, not a defect).
