# Forge 0.552 findings

| ID | Status | Finding |
|---|---|---|
| F-552-001 | CORRECTED | The first reachability pass left eight modules unclassified. Explicit durable-goal or shared-runtime classifications were added before the report was accepted. |
| F-552-002 | CORRECTED | The first manifest-path classification left the confidentiality policy, adapters, and third-party license directory unclassified. Their package/migration/reference roles are now explicit. |
| F-552-003 | CORRECTED | A CLI-only graph marked standalone evidence modules unreachable. The boundary now includes ordinary CLI roots, public standalone tools, and verification roots separately. |
| F-552-004 | RETAINED TRUTH | All 88 runtime modules are reachable through at least one active surface. This does not prove equal value or forbid later folding; it forbids deletion based only on incomplete reachability. |
| F-552-005 | CORRECTED | Narrative-integrity fixtures still supplied 517 regressions and 53 modules after the new reachability test module was added. They now bind 523 regressions across 54 modules. |
| F-552-006 | CORRECTED | The first website generation invocation supplied the destination checksum file as its own source and triggered a same-file refusal. Final generation uses a separate checksum source and preserves the generator boundary. |

| F-552-007 | CORRECTED | The first public-package run expected the development-only generated reachability JSON. Public tests now recompute the same bounded map when the development report is intentionally absent. |
| F-552-008 | DISPROVED OPERATOR ERROR | The first browser verification call passed a website file path into the digest argument through argparse abbreviation. Exact SHA-256 verification passes; no browser-evidence binding defect was recorded. |
| F-552-009 | CORRECTED | The public-edition reachability regression assumed the development-only report was packaged. It now verifies the same bounded map directly when the report is intentionally excluded. |

Open findings: **0**.
