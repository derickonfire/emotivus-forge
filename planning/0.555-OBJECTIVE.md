# Forge 0.555 objective

Complete Durable Core chunks **P1-06** ("remove only modules made unreachable by
completed folds, and retire tests that verify removed ceremony rather than
retained behavior") and **P1-07** ("rebuild public/development boundaries and
prove no retained behavior or history was lost"), closing the core-reduction
phase (P1).

0.555 is **sealed** on top of 0.554. Both chunks are **COMPLETE**; the active
chunk advances to **P2-01** (Goal 1: define the minimal project-truth schema).
The certified suite is unchanged at 523/523 across 54 modules.

## P1-06 — retire fold-orphaned code (conservatively)

The P1-04 and P1-05 folds routed overlapping services through *existing* shared
modules (`common.py`, `project_identity.py`), so no whole module became
unreachable — confirmed: reachability holds at 88 modules, zero statically
unreachable, zero unclassified. Per the chunk's own rule ("remove **only** modules
made unreachable by completed folds"), nothing was deleted on reachability alone.

What the folds did leave was dead imports. An AST scan identified the
fold-orphaned imports and they were retired: `stat` / `ZIP_TIMESTAMP` (evidence
kit), `hashlib` / `stat` / `ZIP_TIMESTAMP` (attestation kit), `hashlib` / `stat`
(external evidence), `hashlib` (browser evidence, artifact collision, narrative
integrity), `read_json` (capabilities), and the now-unused
`project_identity_record` / dead `_current_build_id` imports across the three
release modules. `release_proof` was found to carry a `_current_build_id` helper
that was never called even before the fold — removed.

No test asserted the removed ceremony requirements (`native_advantage`,
`focused_regressions`, `allow_repairs`, required `authority`/`scope`), so there was
no ceremony-only test to retire. Two genuinely pre-existing unused imports
(`base64` in external evidence, `re` in release package) are **not** fold-caused
and were deliberately left in scope for a later dedicated dead-code pass, honoring
P1-06's conservative boundary.

## P1-07 — prove the edition boundary

The public and development editions were rebuilt from the current post-fold source
and independently proven behavior-preserving:

- **Development edition** (the repository): full suite 523/523 across 54 modules.
- **Public edition**: the public `RUN-FORGE.zip` was built, extracted, and its own
  bundled suite run from the extracted bytes — 523/523 across 54 modules, and
  `python3 forge.py` runs. This proves the public boundary carries every folded
  dependency (`common.py`'s shared helpers included) with no lost behavior.
- **Website-embedded runtime**: byte-identical to the public runtime (enforced by
  narrative integrity's checksum binding).
- **No history lost**: git history retains every fold as a diffable commit,
  `docs/history/` retains the four relocated documents, and `CHANGELOG.md`
  preserves every version entry.

## Boundary preserved

Forge still supplies verified context and continuity; the model owns reasoning and
implementation. No `NOT_RUN` was upgraded to `PASS`; release authorization remains
`false`.
