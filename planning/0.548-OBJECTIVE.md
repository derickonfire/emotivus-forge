# Forge 0.548 objective — exact browser viewport evidence

## Current objective

Implement a deterministic, public-neutral browser evidence instrument for the exact Forge website package. It must safely extract one exact website ZIP, serve it only on loopback, drive an operator-supplied Chromium-family browser through a bounded protocol, capture route and viewport render facts plus content-addressed screenshots and DOM snapshots, record browser/platform identity, and preserve explicit limits: local Linux viewport evidence is not a physical-device test, accessibility audit, independent review, owner authorization, or release readiness.

## Exact next action

Record pre-correction findings, implement and test the bounded browser evidence runner, execute it against the exact final Forge website package, integrate the resulting receipt into certification without promoting it beyond its actual Linux/headless-browser tier, then rebuild and certify every exact package surface.

## Authority

Owner continuation instruction in the active session on 2026-08-04. This file records the implementation objective only; it does not authorize release or invent external evidence.
