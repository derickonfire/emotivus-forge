# Forge 0.544 findings register

Status: **CLOSED — 8 recorded, 8 dispositioned, 0 open.**

## F-544-001 — sealed handoff gives an invalid self-host startup command

The 0.543 outer `session/NEW-CHAT-PROMPT.md` instructs the next agent to extract the development
archive and run `python3 forge.py` from the extracted `Emotivus-Forge/` source root. That path is
itself treated as the Forge runtime. The exact instructed command returned `FORGE NOT STARTED`
because no host-project files existed beside the runtime. Forge self-development instead requires
a distinct target-project root containing the exact public runtime in a nested
`Emotivus-Forge/` directory, followed by `python3 Emotivus-Forge/forge.py` from the target root.

## F-544-002 — artifact collision observation does not honor project-owned `.forgeignore`

A controlled 0.543 campaign placed rival same-name, same-version ZIPs beneath a path explicitly
excluded by `.forgeignore`. Orientation correctly honors the project exclusion, but
`observe_version_collisions` walked the ignored path anyway and returned `FAIL`. In Forge's own
source this also scanned the cold `capability_vault` reference archive and blocked Run Forge on a
historical filename/manifest spelling difference. Ordinary observation must respect the same
project-owned exclusion boundary without weakening collision checks on active paths.

## F-544-003 — the Forge source has no self-development exclusion contract for examples

A fresh self-adoption selected `examples/BACKLOG.example.md` as the current objective and generated
a high-impact deadline-precision decision fork from synthetic example prose. The example is useful
packaged documentation, but it is not project authority. The Forge source needs a project-owned
`.forgeignore` that excludes examples and the cold capability vault from ordinary self-development
orientation while retaining them in deterministic package construction.

## F-544-004 — integrity controls lack one reproducible campaign receipt

The 0.543 controls have focused unit regressions, but the roadmap-authorized validation sequence did
not yet produce one current-build campaign receipt covering an external writer after checkpoint,
actual package-source drift before publication, rival exact-byte artifacts, filename/manifest
version disagreement, and explicit quarantine recovery. Without a unified receipt, later sessions
must reconstruct whether those scenarios were actually exercised against the same build.

## F-544-005 — high-impact decision-fork detection can self-trigger from detector prose

After the ignore-boundary correction, a self-host Run Forge pass still reconstructed the
`deadline-precision` fork from Forge's own changelog, detector documentation, detector source, and
test fixtures. The detector treated every Markdown file as authority and allowed implementation
text alone to supply the requested capability. A high-impact pending decision must require an
active authority-backed requirement signal; implementation text may corroborate it but must not
create product authority by itself.

## F-544-006 — one outer self-test invocation appeared stalled before buffered output arrived

A bounded outer command initially reported a timeout while the Forge self-test subprocess continued
running. The exact process completed all 478 assertions and then correctly failed only narrative
metadata that still declared 475 / 47. A subsequent inspection showed module progress was buffered by
the outer execution surface, not lost by Forge. No deterministic Forge lifecycle defect was
reproduced, so no speculative runner change is justified. Final certification must still obtain a
clean bounded self-test exit from the final bytes.

## F-544-007 — narrative current-source regression fixture retained 0.543 totals

After the 0.544 test inventory advanced to 478 assertions across 48 modules, the current-source
narrative test still supplied observed totals 475 / 47. The production narrative gate correctly
rejected the stale fixture. The fixture must derive or declare the current certified totals before
final certification.

## F-544-008 — README used noncanonical module-count emphasis

The 0.544 README rendered the module count as `**48**`, while the structural narrative contract
requires the canonical phrase `across 48 deterministic isolated modules`. The prose was human-clear
but machine-inconsistent. The README must use the certified canonical form rather than weakening the
checker.

## Closure summary

| Finding | Final disposition |
|---|---|
| F-544-001 | Corrected `RUN-FORGE.md`, Quickstart, and the continuation contract to require a distinct nested public runtime for Forge self-development. |
| F-544-002 | Artifact collision observation now applies the project-owned `.forgeignore` boundary. |
| F-544-003 | Added a Forge source `.forgeignore` that excludes inactive self-development surfaces from ordinary authority orientation while retaining manifest-selected package inputs. |
| F-544-004 | Added `forge-integrity-campaign/1`; the current 0.544 receipt passes 6/6 scenarios. |
| F-544-005 | High-impact decision forks now require an active authority-backed requirement signal. |
| F-544-006 | Closed as an outer-execution buffering observation; final bounded source self-test exited 0 with 478/478 across 48 modules. No speculative runner change was made. |
| F-544-007 | Updated the current-source narrative fixture to the certified 478 / 48 totals. |
| F-544-008 | Restored the README's canonical machine-checked count phrase without weakening narrative integrity. |

No finding is waived as release authority. Final package certification remains separate from this
source-cycle closure.
