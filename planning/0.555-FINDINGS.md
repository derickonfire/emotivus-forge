# Forge 0.555 findings

| ID | Status | Finding |
|---|---|---|
| F-555-001 | RETAINED TRUTH | No whole module was made unreachable by the P1-04/P1-05 folds, because they routed overlapping services through existing shared modules. Reachability holds at 88 modules, zero statically unreachable, zero unclassified. Per P1-06's rule, nothing was deleted on reachability alone. |
| F-555-002 | CORRECTED | The folds left fold-orphaned imports (`stat`, `hashlib`, `ZIP_TIMESTAMP`, `read_json`, and the now-unused `project_identity_record` imports across the release modules). An AST scan identified them and they were retired; the full suite stays 523/523. |
| F-555-003 | CORRECTED | `release_proof` carried a `_current_build_id` helper that was never called even before the fold (a latent dead helper). The fold surfaced it as an unused import, which was removed. |
| F-555-004 | RETAINED TRUTH | No test asserted the removed ceremony requirements (`native_advantage`, `focused_regressions`, `allow_repairs`, required `authority`/`scope`), so there was no ceremony-only test to retire — the P1-05 reduction preserved every behavior test. |
| F-555-005 | RETAINED TRUTH | Two genuinely pre-existing unused imports (`base64` in `external_evidence`, `re` in `release_package`) are not fold-caused; they were deliberately left for a later dedicated dead-code pass, honoring P1-06's conservative "remove only what folds orphaned" boundary. |
| F-555-006 | RETAINED TRUTH | P1-07: the public edition, built from post-fold source and run from its own extracted bytes, passes the full 523/54 suite and runs `python3 forge.py` — proving the public/development boundary carries every folded dependency with no lost behavior. No history was lost: git history, `docs/history/`, and `CHANGELOG.md` retain every fold and prior version. This closes the core-reduction phase. |

Open findings: **0**.
