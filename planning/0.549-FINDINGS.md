# Forge 0.549 findings — recommendation re-verification and unbaselined truth state

Status: **CLOSED**. Sealed 0.548 artifacts remain immutable. The source-cycle register contains 19 findings: 10 corrected, 3 disproved, 1 not adopted, and 5 retained roadmap gaps or signals. Open findings: **0**.

| ID | Final disposition | Finding and disposition |
|---|---|---|
| F-549-001 | CORRECTED | `forge check` returned top-level `PASS` while the authority baseline was `NOT_ESTABLISHED`. Aggregate status is now `NOT_RUN` unless authority eligibility exists; real defects remain `FAIL`. |
| F-549-002 | CORRECTED | `--checkpoint` could advance after that unbaselined aggregate PASS. A `NOT_RUN` Check can no longer checkpoint. |
| F-549-003 | CORRECTED | Aggregate claim and rendering supported only PASS/FAIL wording. `Scoped Check NOT_RUN`, aggregate eligibility, resolving artifact, resolving command, and bounded human rendering are now implemented. |
| F-549-004 | CORRECTED | Ship's latest-Check prerequisite could be satisfied by the misleading unbaselined PASS. `last-check-pass` now remains `NOT_RUN` until an authority-eligible Check actually passes. |
| F-549-005 | DISPROVED | No-argument routing was said to be absent. Forge 0.548 already routed among `ADOPT + RESUME`, `RESUME`, and `SCOPED CHECK + RESUME`. |
| F-549-006 | DISPROVED | Forge's browser-evidence tool was said never to have run. The sealed 0.548 bundle re-verifies against the exact delivered website and records 12 renders. Server-rendered/origin evidence remains separate. |
| F-549-007 | DISPROVED | Release Proof was called uncertified. Forge 0.548 contains active-core implementation, dedicated regressions, Ship integration, and bounded documentation. Actual release evidence and authorization remain `NOT_RUN`. |
| F-549-008 | NOT ADOPTED | Wholesale deletion of capability contracts is unsupported by the testimony. Current contracts materially enforce scope, evidence paths, budgets, no-repair behavior, fingerprint reactivation, and Runtime Proof recipes. |
| F-549-009 | RETAINED GOVERNANCE GAP | The seven-axis percentage is implementation-and-instrument coverage, not a defect-catch score. 0.549 labels it accordingly; an efficacy replacement requires a real scored trial. |
| F-549-010 | RETAINED PRODUCT GAP | The exact-token benchmark has no obtainable `OBSERVED` comparison tier, and no general caught/missed/false-positive scoreboard exists. No scored six-defect result was admitted. |
| F-549-011 | RETAINED BLOAT SIGNAL | Active development `docs/` contained 62 Markdown files and approximately 22,658 words. Reduction remains deferred until dependency and required-reference mapping exists. |
| F-549-012 | RETAINED USABILITY GAP | Session Close data is strong, but public use requires many `check --close-session` flags. A sixth `close` command remains rejected; a file-backed Check wrapper may be evaluated later. |
| F-549-013 | CORRECTED | The portable writer workflow depended on an unbaselined passing checkpoint. It now observes `NOT_RUN`, records exact-fingerprint authority in a separate Adopt operation, then requires a passing checkpoint before the writer step. |
| F-549-014 | CORRECTED | The six-scenario integrity campaign also depended on an unbaselined passing checkpoint. Its external-writer scenario now uses the same explicit authority sequence. |
| F-549-015 | CORRECTED | Eighteen existing regression modules encoded aggregate PASS for component-only clean observations without authority. Those assertions now preserve component PASS/WARN/NOT_RUN evidence while expecting aggregate `NOT_RUN`, or explicitly authorize exact fixtures when checkpoint/Ship behavior is the subject. |
| F-549-016 | CORRECTED | One Run Forge presentation-profile fixture depended on the accidental unbaselined PASS route. It now establishes exact authority and a passing checkpoint before asserting continuation-prompt behavior. |
| F-549-017 | CORRECTED | `FORGE-PRODUCT.json` retained `display_version: 0.548` after the canonical version advanced to 0.549, causing generated website footers and status output to misname the current candidate. The display version now matches the canonical package version, and the site was regenerated before packaging. |
| F-549-018 | CORRECTED | The narrative-integrity current-source regression fixture still supplied the prior 510-test / 52-module certification boundary after 0.549 advanced to 515 / 53. The fixture now uses the canonical current counts while retaining a deliberately stale negative case. |
| F-549-019 | CORRECTED | The progress-drift regression attempted to mutate a `96%` roadmap literal that no longer existed after axis 3 advanced to 97%, so the test did not inject drift. It now mutates the current 97% literal and again proves detection. |

## Testimony identity

- Preserved source: `research/FORGE-FINAL-RECOMMENDATION-TESTIMONY.md`
- SHA-256: `f62c029d9dfc8f3137dade2ad99930dd105ac178159575d2356f756f8e031d23`
- Classification: owner-supplied testimony and roadmap advice; not an admissible field observation or scored ledger result.

## Scope boundary

0.549 corrects only the directly reproduced authority/aggregate truth defect and the workflows and regressions that depended on it. It does not delete capability safety contracts, add a sixth public command, claim a field trial, create an efficacy score, or authorize release.
