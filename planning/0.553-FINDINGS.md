# Forge 0.553 findings

| ID | Status | Finding |
|---|---|---|
| F-553-001 | CORRECTED | Once Forge lives under version control, the runtime reachability mapper flagged `.git/` as an unclassified, REVIEW-disposition top-level path, failing `test_every_active_top_level_path_is_classified`. Version-control and CI metadata (`.git/`, `.github/`) are now skipped exactly as `.forge/` state and build output already are, and `.gitignore` / `.gitattributes` are classified `PACKAGING, REFERENCE` (KEEP). Suite restored to 523/523 across 54 modules. |
| F-553-002 | RETAINED TRUTH | The certified 0.552 base was inherited unchanged and independently re-verified in this session: `python3 forge.py` produces a coherent Brief; `python3 -m unittest discover -s tests` reports 523/523 in ~45s with no freeze. The earlier build's interpreter-shutdown stall (fixed by the 0.552 self-test runner's bounded process-group teardown) did not recur. |
| F-553-003 | OBSERVED MISS | Forge is invoked only as a human-typed CLI (`python3 forge.py`). In a multi-agent, multi-vendor 2029, the natural surface is a capability agents *call* automatically. This is recorded as the ground-truth miss that authorizes Phase B (agent-native invocation) under the expansion rule. No instrument is built in 0.553; the miss is declared so a bounded implementation and scored trial can follow. |

| F-553-004 | CORRECTED | The staged 0.553 objective assumed relocating docs would desync the manifest before the version bump. Investigation disproved this: `docs/` is treated as a path *prefix* by the manifest (`canonical_paths`/`public_paths`), the reachability classifier (top-level-name only), and the top-level inventory (nesting-invariant file counts). Chunk **P1-03** was therefore landed in 0.553: `DURABLE-CORE-ROADMAP.md`, `LINEAGE-SAFETY-AMENDMENT.md`, `VERSIONING.md`, and `PRESENTATION-CONTRACT.md` (each zero-reference, historical/explanatory-only) were relocated into `docs/history/` with `git mv`, no history lost and no self-check disturbed. The active chunk advanced to P1-04. |
| F-553-005 | RETAINED TRUTH | The 0.552 → 0.553 version bump was performed in lockstep across the nine coupled surfaces plus the public package, checksums, and site, with the certified regression suite held at 523/523 across 54 modules (no behavior added or removed). Independent re-verification: full suite green; `narrative_integrity` and `check_progress` PASS; live Brief coherent; release authorization remains `false`.

Open findings: **0** (F-553-003 is a recorded miss authorizing future work, not a defect).
