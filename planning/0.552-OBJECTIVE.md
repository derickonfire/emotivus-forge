# Forge 0.552 objective

Complete P1-01 and P1-02 before removing code: generate the active runtime import and command reachability map, classify every active top-level path, and preserve the rule that observation alone does not authorize deletion.
