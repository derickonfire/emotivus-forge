# Forge 0.549 objective — unbaselined truth-state correction and recommendation audit

## Current objective

Re-verify the owner-supplied `FORGE-FINAL-RECOMMENDATION.md` against the exact sealed Forge 0.548 source and artifacts, preserve it as testimony rather than evidence, and implement the first load-bearing defect that the audit confirms: an unbaselined project tree must never receive a top-level `Scoped Check PASS` or a passing checkpoint.

The change must preserve useful sub-check observations while making the aggregate Check status, claim, exit behavior, checkpoint behavior, Resume/sidecar state, and Ship dependency truthful. It must not delete capability safety contracts, claim an external field trial, replace the five-command public interface, or manufacture a scored defect result.

## Exact next action

Record the recommendation audit and pre-correction findings; add regression coverage for unbaselined unchanged, changed-clean, changed-defective, and explicitly authorized trees; implement aggregate `NOT_RUN` semantics and resolving guidance; run the complete exact-byte certification; then package 0.549 only if every source and distribution gate closes.

## Authority

Owner supplied `FORGE-FINAL-RECOMMENDATION.md` and continued the Forge roadmap in the active session on 2026-08-04. The recommendation itself explicitly requires re-verification before becoming a contract. This objective authorizes the bounded verified correction above, not wholesale scope deletion or release authorization.
