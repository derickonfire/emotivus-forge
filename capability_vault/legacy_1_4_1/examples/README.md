# Setup Examples

Copy `forge-setup.example.json` outside the Forge folder, edit it for the project, and run:

```bash
python3 Emotivus-Forge/forge.py init . --answers forge-setup.json
```

Do not place credentials, API keys, passwords, private URLs, or production data in the setup answers file.
