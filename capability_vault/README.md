# Forge Cold Capability Vault

This directory preserves the audited 1.4.1-dev implementation as research and migration material. It is not an active Python package and is not imported by Help, Adopt, Resume, ordinary Check, or Run Forge.

`manifest.json` is the controlling legacy inventory. The clean-room Forge Doctor service in the active core does not import or execute the legacy Doctor or remediation modules. Those legacy files remain frozen research material.

Any later reconnection requires a real project trigger, a certified active implementation, a project-owned activation contract, focused neutral regressions, bounded resources, exact scope and exclusions, and demonstrated value distinct from the project’s native tool.

The original development archive remains under `references/` with its SHA-256 checksum in the manifest.
