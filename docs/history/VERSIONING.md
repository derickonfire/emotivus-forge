# Versioning

Forge uses owner-declared incremental development builds.

The current packaged build is **0.549**. Each newly packaged build advances by `0.001` unless the owner declares otherwise.

A version label is never sufficient identity. Forge separately binds build identity, normalized tree identity, parent package identity, migration identity, package-family identity, exact artifact bytes, and current evidence.
